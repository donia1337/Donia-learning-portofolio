# You can insert a variable into a string by using f-strings.
# The syntax is simple, just insert the variable in-between a set of curly braces {}.

# for example:


score = 1
height = 1.9
isWinning = True

print(f"Your score is {score}, your height is {height}, you are winning {isWinning}")